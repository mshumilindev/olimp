import React, { useEffect } from 'react';

export default function Chatroom() {
    useEffect(() => {

    }, []);

    return (
        <div>
        </div>
    );

    function sendMessage(message) {
        console.log(message);
    }
}
